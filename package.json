{
  "name": "interview-prep-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Turns a job description into a personalised interview preparation kit",
  "workspaces": [
    "packages/core",
    "packages/api",
    "packages/web"
  ],
  "scripts": {
    "evaluate": "tsx packages/core/src/cli/evaluate.ts",
    "dev": "npm-run-all --parallel dev:api dev:web",
    "dev:api": "npm run -w @ipk/api dev",
    "dev:web": "npm run -w @ipk/web dev",
    "test": "npm run -w @ipk/core test && npm run -w @ipk/api test",
    "test:core": "npm run -w @ipk/core test",
    "test:api": "npm run -w @ipk/api test",
    "typecheck": "npm run -w @ipk/core typecheck && npm run -w @ipk/api typecheck && npm run -w @ipk/web typecheck",
    "build": "npm run typecheck && npm run -w @ipk/web build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
